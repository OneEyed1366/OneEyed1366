import 'package:flutter/cupertino.dart';

class UiCupertino extends StatelessWidget {
  const UiCupertino({ Key? key }): super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('its cupertino');
  }
}
