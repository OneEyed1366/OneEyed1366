import 'package:flutter/material.dart';

class UiMaterial extends StatelessWidget {
  const UiMaterial({ Key? key }): super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Its material');
  }
}
